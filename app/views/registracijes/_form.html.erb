<%= form_for @registracije, :html => { :class => "form-horizontal registracije" } do |f| %>

  <% if @registracije.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@registracije.errors.count, "error") %> prohibited this registracije from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @registracije.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :car_id, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :car_id, :class => 'form-control' %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :car_exp_date, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :car_exp_date, :class => 'form-control' %>
    </div>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                registracijes_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
