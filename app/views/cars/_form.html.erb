<%= form_with(model: car, local: true) do |form| %>
  <% if car.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(car.errors.count, "error") %> prohibited this car from being saved:</h2>

      <ul>
        <% car.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_area :user_id %>
  </div>

  <div class="field">
    <%= form.label :plate %>
    <%= form.text_area :plate %>
  </div>

  <div class="field">
    <%= form.label :registration_date %>
    <%= form.text_area :registration_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
